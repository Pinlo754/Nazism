"use client";
import React from "react";
const NavBar = () => {
  return (
    <header className="fixed top-0 z-50 h-21 px-4 sm:px-7 bg-white border-b border-white-active backdrop-blur-sm shadow-[0_0.25rem_0.25rem_0_theme(colors.black/0.25)] font-montserrat w-full">
      Header
    </header>
  );
};

export default NavBar;
